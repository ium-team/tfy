# TFY 0.1.0 Historical Preview Notes

Not supported: provider/API prompt proxying, private Codex hook interception, universal terminal interception, editor auto-hooking, and public external superiority claims without a reviewed benchmark manifest.


## Historical preview install channel

This historical preview can be tested, when the historical package still exists in the registry, through an exact `@ium/tfy-cli` npm version while keeping the user-facing command as `tfy`:

```sh
npm install -g @ium/tfy-cli@0.1.0-preview.1
tfy start --agent
tfy start --human
# Cross-platform human managed-session UX:
tfy human shell
```

GitHub Releases remain the canonical binary source. npm downloads the matching release archive and checksum, verifies the checksum, installs the platform binary, and exposes the `tfy` command. TFY now uses beta/stable as the forward install channels: beta via `@ium/tfy-cli@beta` and stable via plain `@ium/tfy-cli` / npm `latest`. Because this historical release was preview-only, users installed it with the explicit `@preview` tag; maintainers should follow the canonical dist-tag checklist in `docs/RELEASE_READINESS.md` if preview ever appears on `latest`. Historical preview publishes used the `preview` dist-tag and must not imply GA readiness or named-host launch support. Later stable releases must move only reviewed stable versions to the npm `latest` dist-tag, so plain `@ium/tfy-cli` installs remain the safest/stable path. Exact version installs use npm syntax such as historical `@ium/tfy-cli@0.1.0-preview.0`, current beta `@ium/tfy-cli@0.1.1-beta.0`, or stable `@ium/tfy-cli@0.1.1`; npm does not use `@ium/tfy-cli/v0.1.1` for package versions. The repository helper `node scripts/npm-publish-plan.js ...` validates the intended channel and prints the exact npm publish command after the GitHub Release exists; it does not publish by itself.

Supported prebuilt npm/GitHub Release platforms:

| OS | Architecture | Rust target | npm prebuilt |
| --- | --- | --- | --- |
| macOS | Apple Silicon arm64 | `aarch64-apple-darwin` | yes |
| Linux | x64 | `x86_64-unknown-linux-gnu` | yes |
| Linux | arm64 | `aarch64-unknown-linux-gnu` | yes |
| Windows | x64 | `x86_64-pc-windows-msvc` | yes |

Intel Mac (`darwin:x64` / `x86_64-apple-darwin`) is not provided as a prebuilt npm/GitHub Release archive. Intel Mac users can still build from source with `git clone https://github.com/ium-team/tfy && cd tfy && cargo install --path crates/tfy-cli`, or run a locally built binary by setting `TFY_BINARY_PATH`.

## Human managed session update

On supported platform shells (Linux bash, macOS zsh, Windows PowerShell), `tfy start --human` now creates/refreshes trusted current-directory future-shell activation state, offers a default-No prompt to install the missing one-time rc/profile hook for future shells, and enters a TFY-managed current-directory-scoped shell session when run as the only human target from an interactive terminal. New supported shell sessions still require accepting that prompt or running explicit `tfy setup --human --apply`, non-interactive plain start remains intent-only, npm install never edits shell startup files, and ordinary terminals outside managed/marked current directories are still not globally intercepted. Safely resolved ordinary external commands known to the generated shim/proxy use raw-first TFY summary selection; direct-path, explicit bypass, TFY gateway, outside-current-directory-scope commands, and nested child-shell internals run raw without a summary claim; newly created PATH executables may run raw until prompt-time/proxy refresh; automatic interactive/TUI/stateful subcommand classification is not claimed in v1.
