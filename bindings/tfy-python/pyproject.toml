[build-system]
requires = ["maturin>=1.13,<2.0"]
build-backend = "maturin"

[project]
name = "tfy-rust"
version = "0.1.0"
description = "Python binding for the Rust TFY production core"
requires-python = ">=3.9"
